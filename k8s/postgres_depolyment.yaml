apiVersion: apps/v1
kind: Deployment
metadata:
  name: sprakbank-postgres
  labels:
    app: sprakbank-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sprakbank-db
  template:
    metadata:
      labels:
        app: sprakbank-db
    spec:
      containers:
      - name: postgres
        image: postgres:14.4-alpine
        envFrom:
          - configMapRef:
                name: sprakbank-postgres-config
        env:
          - name: POSTGRES_PASSWORD
            valueFrom:
              secretKeyRef:
                name: sprakbank-postgres-secret
                key: POSTGRES_PASSWORD
        ports:
        - containerPort: 5432
          name: sb-db-container
        volumeMounts:
          - name: sprakbank-postgres-vol
            mountPath: /var/lib/postgresql/data
          - name: dshm
            mountPath: /dev/shm
      nodeSelector:
        kubernetes.io/hostname: nbd1k8s7.lx.nb.no
      tolerations:
      - key: "node.kubernetes.io/unschedulable"
        operator: "Exists"
        effect: "NoSchedule"
      volumes:
        - name: sprakbank-postgres-vol
          persistentVolumeClaim:
            claimName: sprakbank-gpu1-pvc
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 1Gi

